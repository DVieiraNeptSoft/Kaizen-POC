if (typeof dateRaised === "undefined" || dateRaised === null || dateRaised === "") { return; }  
var formattedText = dateRaised;

formattedText = convertDate(formattedText);

return formattedText;